// This file is intentionally minimal as the actual root layout
// is at app/[lang]/layout.tsx to support i18n routing
export default function RootLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return children
}
